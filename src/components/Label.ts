import styled from "styled-components";

export const Label = styled.div`
  display: inline-flex;
  align-items: center;
  gap: 0.25rem;
  font-size: 14px;
  svg {
    width: 1rem;
    height: 1rem;
  }
`;
